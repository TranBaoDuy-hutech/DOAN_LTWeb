<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Việt Lữ Travel</title>
    <script type="importmap"></script>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="~/css/enhanced-styles.css" asp-append-version="true" />
    @using Microsoft.AspNetCore.Identity
    @inject SignInManager<IdentityUser> SignInManager
    @inject UserManager<IdentityUser> UserManager

   
</head>
<body class="d-flex flex-column min-vh-100">
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">
                    <img src="~/image/logo.png" alt=" Việt Lữ Travel Logo" />
                    <span class="brand-text">Việt Lữ Travel</span>
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Tour" asp-action="Index">Home</a>
                        </li>
                        
                        <partial name="_LoginPartial" />
                        @if (SignInManager.IsSignedIn(User) && User.IsInRole("Admin"))
                        {
                            <li class="nav-item">
                                <a asp-area="Admin" asp-controller="Tour" asp-action="Index" class="nav-link text-danger fw-bold">
                                    🛠 Trang Admin
                                </a>
                            </li>
                        }
                        @if (SignInManager.IsSignedIn(User) && User.IsInRole("Employee"))
                        {
                            <li class="nav-item">
                                <a asp-area="Employee" asp-controller="Tour" asp-action="Index" class="nav-link text-danger fw-bold">
                                    📋 Quản lý Tour
                                </a>
                            </li>
                        }
                        @if (SignInManager.IsSignedIn(User) && User.IsInRole("Company"))
                        {
                            <li class="nav-item">
                                <a asp-area="Company" asp-controller="Schedule" asp-action="Index" class="nav-link text-danger fw-bold">
                                    📋 Xem lịch Tour
                                </a>
                            </li>
                        }
                        @if (SignInManager.IsSignedIn(User) && User.IsInRole("Customer"))
                        {
                            <li class="nav-item">
                                <a asp-area="Customer" asp-controller="Booking" asp-action="MyBookings" class="nav-link text-danger fw-bold">
                                    🗓 Lịch sử đặt Tour
                                </a>
                            </li>
                        }

                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <div class="container-fluid flex-grow-1">
        <main class="col-12 p-0">
            @RenderBody()
        </main>
    </div>

    <footer class="bg-dark text-light py-4 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <h5 class="mb-3">
                        <i class="fas fa-plane me-2 text-primary"></i>Việt Lữ Travel
                    </h5>
                    <p class="mb-3">
                        Khám phá thế giới cùng chúng tôi. Việt Lữ Travel mang đến những trải nghiệm du lịch tuyệt vời.
                    </p>
                    <div>
                        <a href="#" class="text-light me-3 fs-5"><i class="fab fa-facebook-f"></i></a>
                        <a href="#" class="text-light me-3 fs-5"><i class="fab fa-twitter"></i></a>
                        <a href="#" class="text-light me-3 fs-5"><i class="fab fa-instagram"></i></a>
                        <a href="#" class="text-light me-3 fs-5"><i class="fab fa-youtube"></i></a>
                    </div>
                </div>
                <div class="col-md-6">
                    <h5 class="mb-3">Thông tin liên hệ</h5>
                    <ul class="list-unstyled">
                        <li><i class="fas fa-map-marker-alt me-2 text-primary"></i> Địa chỉ: Khu Công nghệ cao Xa lộ Hà Nội, Hiệp Phú, Thủ Đức, Hồ Chí Minh, Việt Nam</li>
                        <li><i class="fas fa-phone me-2 text-primary"></i> Điện thoại: 0329810650</li>
                        <li><i class="fas fa-envelope me-2 text-primary"></i> Email: baoduy10072004@gmail.com</li>
                    </ul>
                </div>
            </div>

            <hr class="my-4">

            <div class="row align-items-center">
                <div class="col-md-6">
                    <p class="mb-0">© 2025 Việt Lữ  Travel - by Trần Bảo Duy</p>
                </div>
            </div>
        </div>
    </footer>

    <!-- Nút chatbox -->
    <button id="chatboxToggleBtn" class="btn btn-primary rounded-circle" onclick="window.location.href='@Url.Action("Index", "Chat")'">
        <i class="fas fa-robot"></i>
    </button>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>